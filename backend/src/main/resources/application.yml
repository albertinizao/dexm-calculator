server:
  port: ${SERVER_PORT:8084}
spring:
  datasource:
    url: ${DB_URL:jdbc:mariadb://localhost:3306/dexm_personajes}
    username: ${DB_USERNAME:dexm}
    password: ${DB_PASSWORD:KpDC5EvmZPTymucP0pbr}
  jpa:
    hibernate.ddl-auto: validate
    open-in-view: false
  flyway.enabled: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:367992681997-ssehf8iobicne0n2jdnvq9d85dbgu6ke.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: openid,profile,email
app:
  security:
    admin-emails: ${APP_SECURITY_ADMIN_EMAILS:albertinizao@gmail.com}
    frontend-url: ${APP_SECURITY_FRONTEND_URL:http://localhost:5177/}
